# COMP0035 Coursework 1 — NTU Graduate Employment Survey

## Setup

```bash
python -m venv .venv
# Windows
.\.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

pip install -r requirements.txt
```

## Run

```bash
python src/section1.1.py
python src/section1.2.py
python src/section2.0.py
```

- Figures saved in `figs/`
- Cleaned data and prepared tables saved in `data/processed/`
- SQLite database saved as `ntu_employment.sqlite`
